apiVersion: apps/v1
kind: Deployment
metadata: 
  name: civitas-deployment 
spec:
  replicas: 1
  selector:
    matchLabels:
      app: civitas
  template:
    metadata:
      labels:
        app: civitas
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: civitas
          image: ghcr.io/kbcadal/civitas:latest
          ports:
            - containerPort: 3000
